# https://www.robotstxt.org/robotstxt.html
User-agent: *
Disallow:


// const response = await fetch(`http://localhost:3001/user1`, {
      //   method: 'GET',
      //   headers: { 'content-Type': 'application/json' },
      //   credentials: 'include',
      // });
      // const content = await response.json()
      // if (!content.message) {
      //   setDataList(content[0].name)
      // }
